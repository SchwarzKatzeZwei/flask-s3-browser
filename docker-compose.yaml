services:
  flask-s3-browser:
    container_name: flask-s3
    image: comjo/flask-s3:2.0.0
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - ${FLASK_RUN_PORT}:${FLASK_RUN_PORT}
    restart: always
    volumes:
      - ./:/app
    command: gunicorn -c config.py app:app
